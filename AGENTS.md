日本語で答えて
pythonのコード実行はできない

便利メモ
- 起動: `npm run dev`（Vite）。ビルド: `npm run build`、プレビュー: `npm run preview`
- 主要UI: `src/components/MusicArchitect.tsx`（8小節×8音シーケンサ、感情パレット、簡易再生）
- 感情データ: `src/data/degreeEmotions.ts`（度数と動き、感情ラベル定義）
- スタイル: `src/App.css`（ダーク系単ページUI、モバイル想定）
- テストの確認： `chromeDevtools` のMCPを使用可能 (killまで行うこと)
- 追加ドキュメント: `music-architect-spec.md` に現行仕様まとめ済み
